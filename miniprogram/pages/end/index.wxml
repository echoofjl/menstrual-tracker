<view class="end-container">
    <text class="title">请选择经期结束日期</text>
    <picker mode="date" value="{{selectedDate}}" bindchange="onDateChange">
        <view class="date-picker">结束日期：{{selectedDate}}</view>
    </picker>
    <button class="confirm-btn" bindtap="onConfirm">确认结束</button>
</view>
<view class="start-container">
    <text class="title">请选择经期开始日期</text>
    <picker mode="date" value="{{selectedDate}}" bindchange="onDateChange">
        <view class="date-picker">开始日期：{{selectedDate}}</view>
    </picker>
    <button class="confirm-btn" bindtap="onConfirm">确认记录</button>
</view>